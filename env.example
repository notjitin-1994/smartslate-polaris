# Public client-side env vars (Vite exposes variables prefixed with VITE_)

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App URLs
VITE_SITE_URL=http://localhost:5173
VITE_AUTH_REDIRECT_URL=
VITE_GOOGLE_REDIRECT_URL=


# OpenAI (direct browser call - easiest option)
# WARNING: exposes your key to users. Use for prototypes only.
VITE_OPENAI_API_KEY=
# Optional: override base URL (e.g., Azure/OpenRouter). Leave empty for OpenAI.
VITE_OPENAI_BASE_URL=
# Optional: model name (e.g., gpt-5, gpt-4o-mini)
VITE_OPENAI_MODEL=gpt-5

# Anthropic Claude (optional; preferred if provided)
# WARNING: exposes your key to users. Use for prototypes only.
VITE_ANTHROPIC_API_KEY=
# Optional: override base URL
VITE_ANTHROPIC_BASE_URL=
# Optional: model name (e.g., claude-3-5-sonnet-latest)
VITE_ANTHROPIC_MODEL=claude-3-5-sonnet-latest



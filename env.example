# Public client-side env vars (Vite exposes variables prefixed with VITE_)

# Supabase
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App URLs
VITE_SITE_URL=http://localhost:5173
VITE_AUTH_REDIRECT_URL=
VITE_GOOGLE_REDIRECT_URL=


# LLM provider selection (anthropic | openai)
VITE_LLM_PROVIDER=anthropic

# OpenAI (direct browser call - easiest option)
# The browser will call your own /api endpoint; do NOT expose keys client-side.
# These are kept for compatibility but should be unset in production.
VITE_OPENAI_API_KEY=
VITE_OPENAI_BASE_URL=
VITE_OPENAI_MODEL=gpt-5

# Anthropic Claude (preferred if provided)
# The browser will call your own /api endpoint; do NOT expose keys client-side.
VITE_ANTHROPIC_API_KEY=
VITE_ANTHROPIC_BASE_URL=
VITE_ANTHROPIC_MODEL=claude-3-5-sonnet-latest
VITE_ANTHROPIC_MAX_TOKENS=1024


